<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="comguguxmathguxgu.httpsgithub.guxgu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/guxgu_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/guxgu_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".InstructionsActivity"/>
        <activity android:name=".LevelSelectActivity">
        </activity>
        <activity android:name=".PlayLevelActivity">
        </activity>
        <activity android:name=".levelUp">
        </activity>
        <activity android:name=".LevelUp">
        </activity>
    </application>

</manifest>